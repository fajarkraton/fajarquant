# Rust
target/
Cargo.lock
**/*.rs.bk

# Paper build artifacts
paper/*.aux
paper/*.bbl
paper/*.blg
paper/*.log
paper/*.out
paper/*.toc
paper/*.synctex.gz
paper/arxiv_v*/
paper/fajarquant_arxiv_*.tar.gz

# Python (reproducibility scripts)
__pycache__/
*.pyc
.venv/
venv/

# Data caches (large; can be regenerated)
data/kv_cache/*.tar.gz
data/cache/
# Multi-model extraction outputs (V26 Phase C1) — ignore the raw .npy
# files (huge, regeneratable via scripts/extract_kv_cache.py) but KEEP
# the small JSON outputs (metadata.json + comparison_results.json) so
# the GONOGO amendment + paper tables remain verifiable from the repo
# without re-running the extraction.
#
# Pattern: ignore prompt_*/ subdirs and stats.json (large) inside any
# model-specific dir like mistral_7b_50p/, qwen2_7b_dryrun/, etc.
# The committed Gemma 4 E2B paper data at data/kv_cache/prompt_* (no
# parent model dir) is unaffected.
data/kv_cache/*_50p/prompt_*/
data/kv_cache/*_50p/stats.json
data/kv_cache/*_dryrun/prompt_*/
data/kv_cache/*_dryrun/stats.json
data/kv_cache/dryrun_*/

# Phase E E1.1 — Indonesian corpus raw shards (large, regeneratable via
# python/phase_e/scripts/build_id_corpus.py; future: hash-pinned via DVC).
# Per-source manifest JSONs (small) ARE kept so corpus stats stay
# verifiable from the repo without re-running the download.
data/phase_e/corpus_id/**/shard_*.parquet
data/phase_e/corpus_id/**/shard_*.parquet.tmp

# Phase E E1.4 — post-dedup output shards (regeneratable via
# python/phase_e/scripts/dedup_corpus.py from corpus_id/). Per-source
# _manifest.json IS kept (small, post-dedup stats verifiable from repo).
# .progress/ holds the resume state pickle — large, re-derivable.
data/phase_e/corpus_id_dedup/**/shard_*.parquet
data/phase_e/corpus_id_dedup/**/shard_*.parquet.tmp
data/phase_e/corpus_id_dedup/.progress/

# Local HF token (defensive — should live in $HOME, never in repo)
.huggingface_env

# IDE
.vscode/
.idea/
*.swp
*~

# OS
.DS_Store
Thumbs.db

# Local env
.env
paper/mlsys_template/
paper/mlsys2026style.zip
